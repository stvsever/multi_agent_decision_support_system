openai>=1.40.0
flask>=3.0.0
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
matplotlib>=3.7.0

tiktoken>=0.7.0
pydantic>=2.0.0
python-dotenv>=1.0.0
tenacity>=8.2.0
huggingface_hub>=0.20.0
certifi>=2024.0.0

# Optional: dashboard system metrics
psutil>=5.9.0

# Optional: local backend (Transformers)
transformers>=4.51.0
torch>=2.1.0
accelerate>=0.26.0
bitsandbytes>=0.41.0

# Optional: local backend (vLLM, typically CUDA only; Qwen3 needs newer vLLM)
vllm>=0.8.5; sys_platform != 'darwin'

# Optional: tests
pytest>=8.0.0

# Optional: local embeddings
sentence-transformers>=2.2.0
