# Secrets
.env
.env.*
!.env.example
*.pem
*.key

# Local project metadata
.DS_Store
.idea/
.vscode/
.claude/

# Python
__pycache__/
*.py[cod]
*.so
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.venv/
venv/

# Jupyter
.ipynb_checkpoints/

# Runtime artifacts (engine root only; validation/ keeps its own results in git)
/logs/
/results/
/outputs/
*.sqlite3
*.db
src/full_stack/backend/data/pseudo_data/outputs/
validation/**/__pycache__/
validation/**/_cache/
.ipynb_checkpoints/

# Raw downloaded source imaging data (re-fetched by the dataset's own download
# step in seconds; not worth tracking). Extracted/derived outputs are kept.
validation/datasets/NUMERACY_STROKE/data/raw/

# Local manuscript and generated report material
report/
!report/
report/*
!report/objects/
report/objects/*
!report/objects/figures/
report/objects/figures/*
!report/objects/figures/main/
report/objects/figures/main/*
!report/objects/figures/main/MAIN_01_flowchart.png

# Packaging
build/
dist/
*.egg-info/

# Per-dataset raw and processed EEG/MRI data are large; kept local, never pushed.
# The derived feature tables, figures and COMPASS inputs under results/ are tracked.
validation/datasets/*/data/

# Engine per-run work dirs under validation results (runtime only)
validation/**/_compass_work/
validation/**/_work/
